VERSION = "show version"

IPV6_INTERFACES = "show ipv6 interface brief"

IPV6_ROUTES = "show ipv6 route"

IPV6_PROTOCOLS = "show ipv6 protocols"

HOSTNAME = "show running-config | include ^hostname"


ASSESSMENT_COMMANDS = [
    VERSION,
    IPV6_INTERFACES,
    IPV6_ROUTES,
    IPV6_PROTOCOLS,
    HOSTNAME,
]